# Adding a UI to Rock, Paper, Scissors 

### HTML Redesign
    I want the layout to have a centered title - "Play ROCK PAPER SCISSORS!" and three images in a div that are also centered and spaced.

    One image for rock, paper, and scissors. Need to have a button under each image as well. 
    
    Add a div for the results of every playRound function call
    and another div for the results of the console.log calls. 
    Will need to output to it via DOM methods.

    I will make the layout using the flex box concepts I know. 

### Button Logic
    Each button will need an event listener placed on it that 
    can call the playRound function with the correct playerSelection
    every time the button is pressed.
